@model CustomValidationDemo.Models.Emp
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        @using (Html.BeginForm("Index", "Demo"))
        {
            @Html.ValidationSummary()

            @Html.EditorFor(x => x.name)<br />
            @Html.EditorFor(x => x.msg)<br />

            <br /><br />
            <input type="submit" value="click me" />
        }
    </div>
</body>
</html>
